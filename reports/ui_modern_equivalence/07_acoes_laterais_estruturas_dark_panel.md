# Inventario das acoes laterais de estruturas no painel dark

Data de referencia: 2026-07-02

## Objetivo

Mapear as acoes de estruturas disponiveis na UI moderna dark e comparar pontos relevantes com a UI atual.

## Escopo

- UI moderna dark
- painel de VWAP e Payoff dark
- comandos de estrutura
- botoes e callbacks relacionados a estrutura

## Arquivos analisados

- UI/components/terminal_vwap_payoff_dark_panel.py
- ui/components/terminal_vwap_payoff_dark_panel.py
- UI/main_window.py
- ui/main_window.py
- UI/modern/dark_window.py
- ui/modern/dark_window.py

## Ocorrencias encontradas

### UI/components/terminal_vwap_payoff_dark_panel.py

- linha 8: - painel lateral retrátil de estruturas;
- linha 16: das pernas da estrutura.
- linha 34: from repositories.structures_repository import StructuresRepository
- linha 36: StructuresRepository = None
- linha 39: from UI.components.structure_editor_dialog import StructureEditorDialog
- linha 41: StructureEditorDialog = None
- linha 113: # BEGIN AUTO STRUCTURE DECISION HELPERS
- linha 127: # END AUTO STRUCTURE DECISION HELPERS
- linha 142: self.structures: List[Dict[str, Any]] = []
- linha 143: self.selected_structure: Optional[Dict[str, Any]] = None
- linha 153: self.reload_structures()
- linha 197: self.btn_toggle = ctk.CTkButton(
- linha 205: command=self.toggle_structures_panel,
- linha 209: self.btn_reload_fixed = ctk.CTkButton(
- linha 218: command=self.reload_structures,
- linha 220: self.btn_reload_fixed.pack(pady=6, padx=10)
- linha 222: self.btn_new_fixed = ctk.CTkButton(
- linha 231: command=self.new_structure,
- linha 235: self.btn_struct_actions = ctk.CTkButton(
- linha 244: command=self._render_structure_actions,
- linha 248: self.btn_open_fixed = ctk.CTkButton(
- linha 257: command=self.toggle_structures_panel,
- linha 281: text="Selecione uma estrutura no menu lateral para carregar a VWAP e Payoff",
- linha 327: text="COMPONENTES DA ESTRUTURA",
- linha 382: self._set_alerts(["Nenhuma estrutura selecionada."])
- linha 412: def toggle_structures_panel(self) -> None:
- linha 414: self.side.grid_remove()
- linha 421: self._render_structures_list()
- linha 423: def reload_structures(self) -> None:
- linha 424: self.structures = self._load_structures()
- linha 425: self._render_structures_list()
- linha 426: self.on_status(f"{len(self.structures)} estruturas carregadas")
- linha 448: def _find_structures_table(self, schema: Dict[str, List[str]]) -> Optional[str]:
- linha 450: "structures",
- linha 451: "structure",
- linha 452: "option_structures",
- linha 453: "estruturas",
- linha 460: id_col = _first_col(cols, ["id", "structure_id"])
- linha 461: name_col = _first_col(cols, ["name", "nome", "structure_name"])
- linha 468: def _load_structures(self) -> List[Dict[str, Any]]:
- linha 472: table = self._find_structures_table(schema)
- linha 477: id_col = _first_col(cols, ["id", "structure_id"])
- linha 478: name_col = _first_col(cols, ["name", "nome", "structure_name"])
- linha 495: structures: List[Dict[str, Any]] = []
- linha 499: item["name"] = item.get("name") or f"Estrutura {item.get('id')}"
- linha 502: structures.append(item)
- linha 504: return structures
- linha 508: def _render_structures_list(self) -> None:
- linha 512: btn_add = ctk.CTkButton(
- linha 514: text="+ Nova Estrutura",
- linha 519: command=self.new_structure,
- linha 526: text="ESTRUTURAS DISPONÍVEIS",
- linha 532: btn_reload = ctk.CTkButton(
- linha 538: command=self.reload_structures,
- linha 540: btn_reload.pack(fill="x", padx=10, pady=(0, 8))
- linha 548: if not self.structures:
- linha 551: text="Nenhuma estrutura encontrada no app.db",
- linha 558: for structure in self.structures:
- linha 559: sid = structure.get("id")
- linha 560: name = structure.get("name")
- linha 561: asset = structure.get("underlying_asset")
- linha 562: status = structure.get("status")
- linha 564: btn = ctk.CTkButton(
- linha 572: command=lambda s=structure: self.select_structure(s),
- linha 576: def select_structure(self, structure: Dict[str, Any]) -> None:
- linha 577: self.selected_structure = dict(structure)
- linha 579: sid = structure.get("id")
- linha 580: name = structure.get("name")
- linha 581: asset = structure.get("underlying_asset")
- linha 583: legs = self._load_legs(sid)
- linha 584: market = self._load_market(asset)
- linha 585: payoff_points = self._load_payoff_points(sid, legs)
- linha 596: self.on_status(f"Estrutura carregada: ID {sid}")
- linha 598: # Menu lateral fixo: não recolher automaticamente após carregar estrutura.
- linha 602: self._render_structure_actions()
- linha 609: "structure_legs",
- linha 613: "estrutura_pernas",
- linha 620: sid_col = _first_col(cols, ["structure_id", "id_structure", "estrutura_id"])
- linha 628: def _load_legs(self, structure_id: Any) -> List[Dict[str, Any]]:
- linha 637: sid_col = _first_col(cols, ["structure_id", "id_structure", "estrutura_id"])
- linha 666: rows = conn.execute(sql, (structure_id,)).fetchall()
- linha 672: def _load_market(self, asset: Any) -> Dict[str, Any]:
- linha 815: def _load_payoff_points(
- linha 817: structure_id: Any,
- linha 820: persisted = self._load_persisted_payoff_points(structure_id)
- linha 825: def _load_persisted_payoff_points(self, structure_id: Any) -> List[Dict[str, float]]:
- linha 830: sid_col = _first_col(cols, ["structure_id", "id_structure", "estrutura_id"])
- linha 845: rows = conn.execute(sql, (structure_id,)).fetchall()
- linha 976: self.legs_table.delete(item)
- linha 996: self.alerts_box.delete("1.0", "end")
- linha 1016: alerts.append("estrutura sem pernas carregadas")
- linha 1138: ax.set_title("Payoff Combinado da Estrutura", color=MUTED, fontsize=10)
- linha 1154: self.btn_export_payoff_png = ctk.CTkButton(
- linha 1157: command=self.export_payoff_png,
- linha 1177: file_path = filedialog.asksaveasfilename(
- linha 1189: fig.savefig(file_path, dpi=150, bbox_inches="tight")
- linha 1200: f"Erro ao salvar: {exc}",
- linha 1204: # BEGIN AUTO STRUCTURE SIDE ACTIONS
- linha 1223: def _require_selected_structure(self):
- linha 1224: structure = getattr(self, "selected_structure", None)
- linha 1225: if not structure:
- linha 1227: "Estrutura",
- linha 1228: "Selecione uma estrutura antes de executar esta acao.",
- linha 1232: return structure
- linha 1247: button = ctk.CTkButton(
- linha 1254: command=command,
- linha 1259: def _render_structure_actions(self) -> None:
- linha 1260: structure = self._require_selected_structure()
- linha 1261: if not structure:
- linha 1262: self._render_structures_list()
- linha 1267: sid = structure.get("id")
- linha 1268: name = structure.get("name")
- linha 1269: asset = structure.get("underlying_asset")
- linha 1270: status = structure.get("status")
- linha 1274: text="ESTRUTURA ATIVA",
- linha 1295: text="Recalcular Payoff",
- linha 1298: command=self.recalculate_selected_structure,
- linha 1301: self._side_section_title("ESTRUTURA")
- linha 1303: text="Editar pernas",
- linha 1306: command=self.edit_selected_structure,
- linha 1309: text="Duplicar estrutura",
- linha 1312: command=self.duplicate_selected_structure,
- linha 1315: text="Arquivar estrutura",
- linha 1318: command=self.archive_selected_structure,
- linha 1326: command=lambda: self._register_structure_decision("HOLD"),
- linha 1332: command=self._render_adjust_structure_block,
- linha 1338: command=lambda: self._register_structure_decision("CLOSE"),
- linha 1345: command=self._render_structures_list,
- linha 1349: def _render_adjust_structure_block(self) -> None:
- linha 1350: structure = self._require_selected_structure()
- linha 1351: if not structure:
- linha 1356: sid = structure.get("id")
- linha 1357: name = structure.get("name")
- linha 1358: asset = structure.get("underlying_asset")
- linha 1362: text="AJUSTAR ESTRUTURA",
- linha 1383: text="Editar pernas",
- linha 1386: command=self.edit_selected_structure,
- linha 1389: text="Duplicar para ajuste",
- linha 1392: command=self.duplicate_selected_structure,
- linha 1398: command=lambda: self._register_structure_decision("ADJUST"),
- linha 1404: command=self._render_structure_actions,
- linha 1408: def new_structure(self) -> None:
- linha 1409: if StructureEditorDialog is None:
- linha 1411: "Editor indisponivel",
- linha 1412: "StructureEditorDialog nao foi encontrado.",
- linha 1418: dlg = StructureEditorDialog(
- linha 1420: structure_id=None,
- linha 1425: if getattr(dlg, "saved", False):
- linha 1426: self._safe_status("Nova estrutura salva")
- linha 1427: self.reload_structures()
- linha 1428: self._render_structures_list()
- linha 1430: messagebox.showerror("Erro ao criar estrutura", str(exc), parent=self.winfo_toplevel())
- linha 1433: def edit_selected_structure(self) -> None:
- linha 1434: structure = self._require_selected_structure()
- linha 1435: if not structure:
- linha 1438: if StructureEditorDialog is None:
- linha 1440: "Editor indisponivel",
- linha 1441: "StructureEditorDialog nao foi encontrado.",
- linha 1446: sid = structure.get("id")
- linha 1450: dlg = StructureEditorDialog(
- linha 1452: structure_id=sid,
- linha 1457: if getattr(dlg, "saved", False):
- linha 1458: self._safe_status(f"Estrutura ID {sid} atualizada")
- linha 1459: self.reload_structures()
- linha 1462: repo = StructuresRepository(db_path)
- linha 1463: updated = repo.get_structure(sid)
- linha 1465: self.select_structure(updated)
- linha 1469: self._render_structure_actions()
- linha 1471: messagebox.showerror("Erro ao editar estrutura", str(exc), parent=self.winfo_toplevel())
- linha 1474: def duplicate_selected_structure(self) -> None:
- linha 1476: if hasattr(self, "_cmd_duplicate"):
- linha 1477: self._cmd_duplicate()
- linha 1478: self.reload_structures()
- linha 1479: self._safe_status("Estrutura duplicada")
- linha 1483: "Duplicar estrutura",
- linha 1484: "Metodo _cmd_duplicate nao encontrado neste componente.",
- linha 1488: messagebox.showerror("Erro ao duplicar estrutura", str(exc), parent=self.winfo_toplevel())
- linha 1491: def recalculate_selected_structure(self) -> None:
- linha 1492: structure = self._require_selected_structure()
- linha 1493: if not structure:
- ocorrencias adicionais omitidas no relatorio: 26

### ui/components/terminal_vwap_payoff_dark_panel.py

- linha 8: - painel lateral retrátil de estruturas;
- linha 16: das pernas da estrutura.
- linha 34: from repositories.structures_repository import StructuresRepository
- linha 36: StructuresRepository = None
- linha 39: from UI.components.structure_editor_dialog import StructureEditorDialog
- linha 41: StructureEditorDialog = None
- linha 113: # BEGIN AUTO STRUCTURE DECISION HELPERS
- linha 127: # END AUTO STRUCTURE DECISION HELPERS
- linha 142: self.structures: List[Dict[str, Any]] = []
- linha 143: self.selected_structure: Optional[Dict[str, Any]] = None
- linha 153: self.reload_structures()
- linha 197: self.btn_toggle = ctk.CTkButton(
- linha 205: command=self.toggle_structures_panel,
- linha 209: self.btn_reload_fixed = ctk.CTkButton(
- linha 218: command=self.reload_structures,
- linha 220: self.btn_reload_fixed.pack(pady=6, padx=10)
- linha 222: self.btn_new_fixed = ctk.CTkButton(
- linha 231: command=self.new_structure,
- linha 235: self.btn_struct_actions = ctk.CTkButton(
- linha 244: command=self._render_structure_actions,
- linha 248: self.btn_open_fixed = ctk.CTkButton(
- linha 257: command=self.toggle_structures_panel,
- linha 281: text="Selecione uma estrutura no menu lateral para carregar a VWAP e Payoff",
- linha 327: text="COMPONENTES DA ESTRUTURA",
- linha 382: self._set_alerts(["Nenhuma estrutura selecionada."])
- linha 412: def toggle_structures_panel(self) -> None:
- linha 414: self.side.grid_remove()
- linha 421: self._render_structures_list()
- linha 423: def reload_structures(self) -> None:
- linha 424: self.structures = self._load_structures()
- linha 425: self._render_structures_list()
- linha 426: self.on_status(f"{len(self.structures)} estruturas carregadas")
- linha 448: def _find_structures_table(self, schema: Dict[str, List[str]]) -> Optional[str]:
- linha 450: "structures",
- linha 451: "structure",
- linha 452: "option_structures",
- linha 453: "estruturas",
- linha 460: id_col = _first_col(cols, ["id", "structure_id"])
- linha 461: name_col = _first_col(cols, ["name", "nome", "structure_name"])
- linha 468: def _load_structures(self) -> List[Dict[str, Any]]:
- linha 472: table = self._find_structures_table(schema)
- linha 477: id_col = _first_col(cols, ["id", "structure_id"])
- linha 478: name_col = _first_col(cols, ["name", "nome", "structure_name"])
- linha 495: structures: List[Dict[str, Any]] = []
- linha 499: item["name"] = item.get("name") or f"Estrutura {item.get('id')}"
- linha 502: structures.append(item)
- linha 504: return structures
- linha 508: def _render_structures_list(self) -> None:
- linha 512: btn_add = ctk.CTkButton(
- linha 514: text="+ Nova Estrutura",
- linha 519: command=self.new_structure,
- linha 526: text="ESTRUTURAS DISPONÍVEIS",
- linha 532: btn_reload = ctk.CTkButton(
- linha 538: command=self.reload_structures,
- linha 540: btn_reload.pack(fill="x", padx=10, pady=(0, 8))
- linha 548: if not self.structures:
- linha 551: text="Nenhuma estrutura encontrada no app.db",
- linha 558: for structure in self.structures:
- linha 559: sid = structure.get("id")
- linha 560: name = structure.get("name")
- linha 561: asset = structure.get("underlying_asset")
- linha 562: status = structure.get("status")
- linha 564: btn = ctk.CTkButton(
- linha 572: command=lambda s=structure: self.select_structure(s),
- linha 576: def select_structure(self, structure: Dict[str, Any]) -> None:
- linha 577: self.selected_structure = dict(structure)
- linha 579: sid = structure.get("id")
- linha 580: name = structure.get("name")
- linha 581: asset = structure.get("underlying_asset")
- linha 583: legs = self._load_legs(sid)
- linha 584: market = self._load_market(asset)
- linha 585: payoff_points = self._load_payoff_points(sid, legs)
- linha 596: self.on_status(f"Estrutura carregada: ID {sid}")
- linha 598: # Menu lateral fixo: não recolher automaticamente após carregar estrutura.
- linha 602: self._render_structure_actions()
- linha 609: "structure_legs",
- linha 613: "estrutura_pernas",
- linha 620: sid_col = _first_col(cols, ["structure_id", "id_structure", "estrutura_id"])
- linha 628: def _load_legs(self, structure_id: Any) -> List[Dict[str, Any]]:
- linha 637: sid_col = _first_col(cols, ["structure_id", "id_structure", "estrutura_id"])
- linha 666: rows = conn.execute(sql, (structure_id,)).fetchall()
- linha 672: def _load_market(self, asset: Any) -> Dict[str, Any]:
- linha 815: def _load_payoff_points(
- linha 817: structure_id: Any,
- linha 820: persisted = self._load_persisted_payoff_points(structure_id)
- linha 825: def _load_persisted_payoff_points(self, structure_id: Any) -> List[Dict[str, float]]:
- linha 830: sid_col = _first_col(cols, ["structure_id", "id_structure", "estrutura_id"])
- linha 845: rows = conn.execute(sql, (structure_id,)).fetchall()
- linha 976: self.legs_table.delete(item)
- linha 996: self.alerts_box.delete("1.0", "end")
- linha 1016: alerts.append("estrutura sem pernas carregadas")
- linha 1138: ax.set_title("Payoff Combinado da Estrutura", color=MUTED, fontsize=10)
- linha 1154: self.btn_export_payoff_png = ctk.CTkButton(
- linha 1157: command=self.export_payoff_png,
- linha 1177: file_path = filedialog.asksaveasfilename(
- linha 1189: fig.savefig(file_path, dpi=150, bbox_inches="tight")
- linha 1200: f"Erro ao salvar: {exc}",
- linha 1204: # BEGIN AUTO STRUCTURE SIDE ACTIONS
- linha 1223: def _require_selected_structure(self):
- linha 1224: structure = getattr(self, "selected_structure", None)
- linha 1225: if not structure:
- linha 1227: "Estrutura",
- linha 1228: "Selecione uma estrutura antes de executar esta acao.",
- linha 1232: return structure
- linha 1247: button = ctk.CTkButton(
- linha 1254: command=command,
- linha 1259: def _render_structure_actions(self) -> None:
- linha 1260: structure = self._require_selected_structure()
- linha 1261: if not structure:
- linha 1262: self._render_structures_list()
- linha 1267: sid = structure.get("id")
- linha 1268: name = structure.get("name")
- linha 1269: asset = structure.get("underlying_asset")
- linha 1270: status = structure.get("status")
- linha 1274: text="ESTRUTURA ATIVA",
- linha 1295: text="Recalcular Payoff",
- linha 1298: command=self.recalculate_selected_structure,
- linha 1301: self._side_section_title("ESTRUTURA")
- linha 1303: text="Editar pernas",
- linha 1306: command=self.edit_selected_structure,
- linha 1309: text="Duplicar estrutura",
- linha 1312: command=self.duplicate_selected_structure,
- linha 1315: text="Arquivar estrutura",
- linha 1318: command=self.archive_selected_structure,
- linha 1326: command=lambda: self._register_structure_decision("HOLD"),
- linha 1332: command=self._render_adjust_structure_block,
- linha 1338: command=lambda: self._register_structure_decision("CLOSE"),
- linha 1345: command=self._render_structures_list,
- linha 1349: def _render_adjust_structure_block(self) -> None:
- linha 1350: structure = self._require_selected_structure()
- linha 1351: if not structure:
- linha 1356: sid = structure.get("id")
- linha 1357: name = structure.get("name")
- linha 1358: asset = structure.get("underlying_asset")
- linha 1362: text="AJUSTAR ESTRUTURA",
- linha 1383: text="Editar pernas",
- linha 1386: command=self.edit_selected_structure,
- linha 1389: text="Duplicar para ajuste",
- linha 1392: command=self.duplicate_selected_structure,
- linha 1398: command=lambda: self._register_structure_decision("ADJUST"),
- linha 1404: command=self._render_structure_actions,
- linha 1408: def new_structure(self) -> None:
- linha 1409: if StructureEditorDialog is None:
- linha 1411: "Editor indisponivel",
- linha 1412: "StructureEditorDialog nao foi encontrado.",
- linha 1418: dlg = StructureEditorDialog(
- linha 1420: structure_id=None,
- linha 1425: if getattr(dlg, "saved", False):
- linha 1426: self._safe_status("Nova estrutura salva")
- linha 1427: self.reload_structures()
- linha 1428: self._render_structures_list()
- linha 1430: messagebox.showerror("Erro ao criar estrutura", str(exc), parent=self.winfo_toplevel())
- linha 1433: def edit_selected_structure(self) -> None:
- linha 1434: structure = self._require_selected_structure()
- linha 1435: if not structure:
- linha 1438: if StructureEditorDialog is None:
- linha 1440: "Editor indisponivel",
- linha 1441: "StructureEditorDialog nao foi encontrado.",
- linha 1446: sid = structure.get("id")
- linha 1450: dlg = StructureEditorDialog(
- linha 1452: structure_id=sid,
- linha 1457: if getattr(dlg, "saved", False):
- linha 1458: self._safe_status(f"Estrutura ID {sid} atualizada")
- linha 1459: self.reload_structures()
- linha 1462: repo = StructuresRepository(db_path)
- linha 1463: updated = repo.get_structure(sid)
- linha 1465: self.select_structure(updated)
- linha 1469: self._render_structure_actions()
- linha 1471: messagebox.showerror("Erro ao editar estrutura", str(exc), parent=self.winfo_toplevel())
- linha 1474: def duplicate_selected_structure(self) -> None:
- linha 1476: if hasattr(self, "_cmd_duplicate"):
- linha 1477: self._cmd_duplicate()
- linha 1478: self.reload_structures()
- linha 1479: self._safe_status("Estrutura duplicada")
- linha 1483: "Duplicar estrutura",
- linha 1484: "Metodo _cmd_duplicate nao encontrado neste componente.",
- linha 1488: messagebox.showerror("Erro ao duplicar estrutura", str(exc), parent=self.winfo_toplevel())
- linha 1491: def recalculate_selected_structure(self) -> None:
- linha 1492: structure = self._require_selected_structure()
- linha 1493: if not structure:
- ocorrencias adicionais omitidas no relatorio: 26

### UI/main_window.py

- linha 12: from UI.components.structures_list_panel import StructuresListPanel
- linha 13: from UI.components.structure_editor_dialog import StructureEditorDialog
- linha 43: # Loading animation
- linha 44: self._loading_animation_active = False
- linha 45: self._loading_animation_chars = ["", "", "", "", "", "", "", "", "", ""]
- linha 46: self._loading_animation_index = 0
- linha 47: self._loading_payoff = False
- linha 48: self._stop_loading_animation()
- linha 61: # Carregar dados iniciais
- linha 100: on_recalculate=self.recalculate_structure,
- linha 112: # Aba 3: Estruturas (Fase 5 -- alteracao_10)
- linha 113: self._setup_structures_tab(right_notebook)
- linha 134: file_menu.add_command(label="Atualizar Dados", command=self.refresh_data)
- linha 136: file_menu.add_command(label="Exportar CSV...", command=self.export_csv)
- linha 138: file_menu.add_command(label="Sair", command=self.root.quit)
- linha 143: tools_menu.add_command(label="Executar Pipeline", command=self.run_pipeline)
- linha 144: tools_menu.add_command(label="Verificar Bancos", command=self.check_databases)
- linha 146: tools_menu.add_command(label="Limpar Cache", command=self.clear_cache)
- linha 151: help_menu.add_command(label="Sobre", command=self.show_about)
- linha 171: messagebox.showerror("Erro", f"Erro ao aplicar filtros: {e}")
- linha 176: alteracao_36: structure_id é suficiente para carregar payoff -- timestamp não é obrigatório.
- linha 189: # Carregar payoff em background -- apenas structure_id necessário
- linha 190: structure_id = decision_data.get("structure_id")
- linha 193: if structure_id is not None:
- linha 194: self._start_payoff_load(structure_id, timestamp, decision_data)
- linha 199: def _start_payoff_load(
- linha 201: structure_id,
- linha 206: alteracao_36: structure_id é a única chave obrigatória.
- linha 209: decision_data = {"structure_id": structure_id}
- linha 214: if self._loading_payoff:
- linha 219: self._loading_payoff = True
- linha 221: def load_worker():
- linha 224: structure_id, timestamp
- linha 228: f"payoff structure_id={structure_id} ts_req={timestamp} "
- linha 256: self._finish_payoff_load,
- linha 271: thread = threading.Thread(target=load_worker, daemon=True)
- linha 276: alteracao_36: preserva seleção usando structure_id como chave -- timestamp é auxiliar.
- linha 283: self.filters_panel.update_structures(
- linha 284: self.data_model.get_structures()
- linha 301: target_sid = d.get("structure_id")  # chave canônica
- linha 304: # Reselecionar na grid: structure_id é suficiente
- linha 317: self._start_payoff_load(target_sid, target_ts, d)
- linha 337: messagebox.showerror("Erro", f"Erro ao carregar dados: {e}")
- linha 338: self.status_bar.config(text="Erro ao carregar dados")
- linha 343: filename = filedialog.asksaveasfilename(
- linha 355: def recalculate_structure(self, structure_id: str):
- linha 357: Recalcula a estrutura identificada por structure_id e atualiza a UI.
- linha 363: text=f"Recalc já em andamento; ignorando ({structure_id})"
- linha 379: self.status_bar.config(text=f"Recalculando {structure_id}...")
- linha 392: structure_id, ok=ok, message=msg
- linha 420: 0, lambda: finish(True, f"OK: {structure_id} recalculado")
- linha 508: Desenvolvido para análise de estruturas de opções
- linha 524: def _finish_payoff_load(
- linha 535: self._loading_payoff = False
- linha 536: self._stop_loading_animation()
- linha 573: self._loading_payoff = False
- linha 574: self._stop_loading_animation()
- linha 579: self.status_bar.config(text=f"Erro ao carregar payoff: {error_msg}")
- linha 585: # Loading animation
- linha 588: def _start_loading_animation(self, base_text: str):
- linha 589: self._loading_animation_active = True
- linha 590: self._loading_animation_index = 0
- linha 593: if not self._loading_animation_active:
- linha 595: char = self._loading_animation_chars[self._loading_animation_index]
- linha 597: self._loading_animation_index = (
- linha 598: self._loading_animation_index + 1
- linha 599: ) % len(self._loading_animation_chars)
- linha 604: def _stop_loading_animation(self):
- linha 605: self._loading_animation_active = False
- linha 608: # Aba Estruturas (Fase 5 -- alteracao_10)
- linha 611: def _setup_structures_tab(self, notebook: ttk.Notebook):
- linha 612: """Aba 'Estruturas' no notebook principal."""
- linha 614: notebook.add(outer, text=" Estruturas")
- linha 623: self.structures_list = StructuresListPanel(
- linha 625: on_structure_selected=self._on_structure_selected,
- linha 626: on_request_edit=self._on_structure_edit_request,
- linha 629: self.structures_list.pack(fill="both", expand=True)
- linha 644: def _on_structure_selected(self, structure: Optional[Dict]):
- linha 645: """Exibe detalhes da estrutura selecionada no painel direito."""
- linha 648: txt.delete("1.0", "end")
- linha 650: if structure is None:
- linha 651: txt.insert("end", "Nenhuma estrutura selecionada.")
- linha 653: legs = structure.get("legs", [])
- linha 655: f"ID         : {structure.get('id')}",
- linha 656: f"Nome       : {structure.get('name')}",
- linha 657: f"Ativo      : {structure.get('underlying_asset')}",
- linha 658: f"Aba legado : {structure.get('alias_legacy_aba') or '--'}",
- linha 659: f"Status     : {structure.get('status')}",
- linha 660: f"Criado em  : {str(structure.get('created_at', ''))[:19]}",
- linha 661: f"Atualizado : {str(structure.get('updated_at', ''))[:19]}",
- linha 662: f"Obs        : {structure.get('notes') or '--'}",
- linha 678: def _on_structure_edit_request(self, structure_id: Optional[int]):
- linha 680: dlg = StructureEditorDialog(
- linha 682: structure_id=structure_id,
- linha 686: if dlg.saved:
- linha 687: self.structures_list.load()
- linha 690: self.status_bar.config(text="Estrutura salva com sucesso.")
- linha 705: from repositories.structures_repository import StructuresRepository
- linha 720: structure_repository = StructuresRepository(db_path)
- linha 722: structure_repository=structure_repository,

### ui/main_window.py

- linha 12: from UI.components.structures_list_panel import StructuresListPanel
- linha 13: from UI.components.structure_editor_dialog import StructureEditorDialog
- linha 43: # Loading animation
- linha 44: self._loading_animation_active = False
- linha 45: self._loading_animation_chars = ["", "", "", "", "", "", "", "", "", ""]
- linha 46: self._loading_animation_index = 0
- linha 47: self._loading_payoff = False
- linha 48: self._stop_loading_animation()
- linha 61: # Carregar dados iniciais
- linha 100: on_recalculate=self.recalculate_structure,
- linha 112: # Aba 3: Estruturas (Fase 5 -- alteracao_10)
- linha 113: self._setup_structures_tab(right_notebook)
- linha 134: file_menu.add_command(label="Atualizar Dados", command=self.refresh_data)
- linha 136: file_menu.add_command(label="Exportar CSV...", command=self.export_csv)
- linha 138: file_menu.add_command(label="Sair", command=self.root.quit)
- linha 143: tools_menu.add_command(label="Executar Pipeline", command=self.run_pipeline)
- linha 144: tools_menu.add_command(label="Verificar Bancos", command=self.check_databases)
- linha 146: tools_menu.add_command(label="Limpar Cache", command=self.clear_cache)
- linha 151: help_menu.add_command(label="Sobre", command=self.show_about)
- linha 171: messagebox.showerror("Erro", f"Erro ao aplicar filtros: {e}")
- linha 176: alteracao_36: structure_id é suficiente para carregar payoff -- timestamp não é obrigatório.
- linha 189: # Carregar payoff em background -- apenas structure_id necessário
- linha 190: structure_id = decision_data.get("structure_id")
- linha 193: if structure_id is not None:
- linha 194: self._start_payoff_load(structure_id, timestamp, decision_data)
- linha 199: def _start_payoff_load(
- linha 201: structure_id,
- linha 206: alteracao_36: structure_id é a única chave obrigatória.
- linha 209: decision_data = {"structure_id": structure_id}
- linha 214: if self._loading_payoff:
- linha 219: self._loading_payoff = True
- linha 221: def load_worker():
- linha 224: structure_id, timestamp
- linha 228: f"payoff structure_id={structure_id} ts_req={timestamp} "
- linha 256: self._finish_payoff_load,
- linha 271: thread = threading.Thread(target=load_worker, daemon=True)
- linha 276: alteracao_36: preserva seleção usando structure_id como chave -- timestamp é auxiliar.
- linha 283: self.filters_panel.update_structures(
- linha 284: self.data_model.get_structures()
- linha 301: target_sid = d.get("structure_id")  # chave canônica
- linha 304: # Reselecionar na grid: structure_id é suficiente
- linha 317: self._start_payoff_load(target_sid, target_ts, d)
- linha 337: messagebox.showerror("Erro", f"Erro ao carregar dados: {e}")
- linha 338: self.status_bar.config(text="Erro ao carregar dados")
- linha 343: filename = filedialog.asksaveasfilename(
- linha 355: def recalculate_structure(self, structure_id: str):
- linha 357: Recalcula a estrutura identificada por structure_id e atualiza a UI.
- linha 363: text=f"Recalc já em andamento; ignorando ({structure_id})"
- linha 379: self.status_bar.config(text=f"Recalculando {structure_id}...")
- linha 392: structure_id, ok=ok, message=msg
- linha 420: 0, lambda: finish(True, f"OK: {structure_id} recalculado")
- linha 508: Desenvolvido para análise de estruturas de opções
- linha 524: def _finish_payoff_load(
- linha 535: self._loading_payoff = False
- linha 536: self._stop_loading_animation()
- linha 573: self._loading_payoff = False
- linha 574: self._stop_loading_animation()
- linha 579: self.status_bar.config(text=f"Erro ao carregar payoff: {error_msg}")
- linha 585: # Loading animation
- linha 588: def _start_loading_animation(self, base_text: str):
- linha 589: self._loading_animation_active = True
- linha 590: self._loading_animation_index = 0
- linha 593: if not self._loading_animation_active:
- linha 595: char = self._loading_animation_chars[self._loading_animation_index]
- linha 597: self._loading_animation_index = (
- linha 598: self._loading_animation_index + 1
- linha 599: ) % len(self._loading_animation_chars)
- linha 604: def _stop_loading_animation(self):
- linha 605: self._loading_animation_active = False
- linha 608: # Aba Estruturas (Fase 5 -- alteracao_10)
- linha 611: def _setup_structures_tab(self, notebook: ttk.Notebook):
- linha 612: """Aba 'Estruturas' no notebook principal."""
- linha 614: notebook.add(outer, text=" Estruturas")
- linha 623: self.structures_list = StructuresListPanel(
- linha 625: on_structure_selected=self._on_structure_selected,
- linha 626: on_request_edit=self._on_structure_edit_request,
- linha 629: self.structures_list.pack(fill="both", expand=True)
- linha 644: def _on_structure_selected(self, structure: Optional[Dict]):
- linha 645: """Exibe detalhes da estrutura selecionada no painel direito."""
- linha 648: txt.delete("1.0", "end")
- linha 650: if structure is None:
- linha 651: txt.insert("end", "Nenhuma estrutura selecionada.")
- linha 653: legs = structure.get("legs", [])
- linha 655: f"ID         : {structure.get('id')}",
- linha 656: f"Nome       : {structure.get('name')}",
- linha 657: f"Ativo      : {structure.get('underlying_asset')}",
- linha 658: f"Aba legado : {structure.get('alias_legacy_aba') or '--'}",
- linha 659: f"Status     : {structure.get('status')}",
- linha 660: f"Criado em  : {str(structure.get('created_at', ''))[:19]}",
- linha 661: f"Atualizado : {str(structure.get('updated_at', ''))[:19]}",
- linha 662: f"Obs        : {structure.get('notes') or '--'}",
- linha 678: def _on_structure_edit_request(self, structure_id: Optional[int]):
- linha 680: dlg = StructureEditorDialog(
- linha 682: structure_id=structure_id,
- linha 686: if dlg.saved:
- linha 687: self.structures_list.load()
- linha 690: self.status_bar.config(text="Estrutura salva com sucesso.")
- linha 705: from repositories.structures_repository import StructuresRepository
- linha 720: structure_repository = StructuresRepository(db_path)
- linha 722: structure_repository=structure_repository,

### UI/modern/dark_window.py

- linha 50: app_menu.add_command(label="Atualizar", command=self._reload_panel)
- linha 52: app_menu.add_command(label="Sair", command=self.root.quit)
- linha 55: help_menu.add_command(label="Sobre", command=self._show_about)
- linha 81: def _reload_panel(self) -> None:
- linha 83: if hasattr(self.panel, "reload_structures"):
- linha 84: self.panel.reload_structures()
- linha 85: self.set_status("Estruturas recarregadas")

### ui/modern/dark_window.py

- linha 50: app_menu.add_command(label="Atualizar", command=self._reload_panel)
- linha 52: app_menu.add_command(label="Sair", command=self.root.quit)
- linha 55: help_menu.add_command(label="Sobre", command=self._show_about)
- linha 81: def _reload_panel(self) -> None:
- linha 83: if hasattr(self.panel, "reload_structures"):
- linha 84: self.panel.reload_structures()
- linha 85: self.set_status("Estruturas recarregadas")

## Leitura inicial

Este relatorio e uma etapa de inventario.

Nenhuma alteracao funcional foi aplicada nesta rodada.

A proxima rodada deve classificar as acoes em:

- ja equivalente no modo dark;
- presente na UI atual e ausente no modo dark;
- presente no modo dark mas sem callback seguro;
- presente com callback, mas precisando validacao manual;
- fora de escopo para esta fase.

## Decisao

Apos este inventario, a proxima etapa recomendada e definir o menor patch funcional para fechar a equivalencia das acoes laterais de estruturas.
