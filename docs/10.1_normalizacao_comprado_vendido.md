# 10.1_normalizacao_comprado_vendido

## Objetivo

Eliminar dupla interpretação futura entre os vocabulários de lado da posição:

- COMPRADO / VENDIDO
- LONG / SHORT
- C / V
- aliases legados como COMPRA, VENDA, BUY, SELL, quando aplicável

## Política oficial

| Camada | Vocabulário oficial |
|---|---|
| UI | COMPRADO / VENDIDO |
| API de estruturas | COMPRADO / VENDIDO |
| Repositório de estruturas | COMPRADO / VENDIDO |
| Canonical input de estrutura | COMPRADO / VENDIDO |
| Enriquecimento RTD de estrutura | COMPRADO / VENDIDO |
| Payload técnico de pricing | LONG / SHORT |
| Cálculo/payoff/métricas técnicas | LONG / SHORT, sempre via conversor central |

## Fonte única

A fonte única para normalização semântica de lado da posição é:

    domain/position_side.py

Funções oficiais:

    normalize_position_side(value) -> COMPRADO/VENDIDO
    to_pricing_engine_side(value) -> LONG/SHORT

## Regra principal

- Dentro de domínio/canonical/estrutura/UI, usar COMPRADO / VENDIDO.
- Na borda técnica de pricing/cálculo, converter para LONG / SHORT.
- Evitar normalizadores paralelos.
- Evitar conversões manuais espalhadas.
- Aliases legados podem ser aceitos na entrada, mas devem ser normalizados imediatamente.

---

## 10.1.1 — Entrada/canonical de estrutura

Status: concluído.

Alvo inicial:

    services/structure_input_mapper.py
    ATT/tests/test_structure_input_mapper.py

Objetivo:

- Trocar upper-case simples por normalização semântica.
- Garantir que position_side saia do mapper canonical como COMPRADO ou VENDIDO.

Exemplos esperados:

| Entrada | Saída canonical |
|---|---|
| long | COMPRADO |
| LONG | COMPRADO |
| C | COMPRADO |
| comprado | COMPRADO |
| short | VENDIDO |
| SHORT | VENDIDO |
| V | VENDIDO |
| vendido | VENDIDO |

## 10.1.2 — UI do editor de estrutura

Status: concluído.

Alvo provável:

    ui/components/structure_editor_dialog.py
    ATT/tests/test_structure_editor_dialog.py
    ATT/tests/test_structure_editor_integration.py

Objetivo:

- Exibir COMPRADO / VENDIDO na UI.
- Criar legs novas com COMPRADO.
- Salvar payload normalizado.
- Aceitar e converter valores legados carregados como LONG / SHORT.

## 10.1.3 — Serviços legados

Status: concluído.

Alvos prováveis:

    services/legacy_robo_legs_fallback.py
    services/robo_leg_mapper.py
    repositories/robo_legs_repository.py

Objetivo:

- Remover normalizações manuais duplicadas.
- Evitar fallback silencioso de valor inválido para vendido.
- Usar domain.position_side.

## 10.1.4 — Cálculo/payoff/métricas

Status: concluído.

Alvos prováveis:

    services/calculation_orchestrator.py
    domain/payoff.py
    domain/structure_metrics.py
    domain/calculation_request.py

Objetivo:

- Manter LONG / SHORT somente onde for contrato técnico.
- Usar to_pricing_engine_side.
- Reduzir normalizadores paralelos.

## Critério de aceite da etapa 10.1

A etapa será considerada fechada quando:

1. domain.position_side for a fonte única de conversão relevante.
2. Entrada/canonical de estrutura sair em COMPRADO / VENDIDO.
3. UI de estrutura trabalhar com COMPRADO / VENDIDO.
4. Payload técnico de pricing continuar saindo em LONG / SHORT.
5. Normalizações manuais relevantes forem removidas ou justificadas.
6. Suíte de testes passar.

## Fechamento técnico da etapa 10.1

Status final: concluído.

Evidências:

- Entrada/canonical de estrutura normalizada para `COMPRADO` / `VENDIDO`.
- UI do editor de estrutura trabalhando com `COMPRADO` / `VENDIDO`.
- Serviços legados de robo legs convertendo entradas antigas via `domain.position_side`.
- Cálculo, payoff e métricas centralizados em `domain.position_side.to_pricing_engine_side()` nas bordas técnicas.
- Payload técnico de pricing preservado em `LONG` / `SHORT`.
- Suíte acumulada da etapa executada com sucesso:

    131 passed in 2.39s

Ocorrências remanescentes justificadas:

- `domain/position_side.py`: fonte única de normalização e conversão.
- `domain/calculation_request.py`, `domain/payoff.py`, `domain/structure_metrics.py`: uso técnico interno de `LONG` / `SHORT` para cálculo.
- `repositories/robo_legs_repository.py`: conversão para `cv` legado `C` / `V` na persistência compatível.
- `services/canonical_pricing_facade.py`: inferência técnica de `LONG` / `SHORT` a partir de quantidade assinada para engine de pricing.
- `UI/components/structure_editor_dialog.py`: uso esperado de `COMPRADO` / `VENDIDO` na interface.
- `scripts/apply_fase9_update_tests_atomic_create.py`: script histórico fora do fluxo produtivo.
- `utils/leg_normalizers.py`: utilitário legado mantido por compatibilidade; não alterado nesta etapa para evitar impacto fora do fluxo 10.1.

## Validação regressiva pós-ajuste de testes legados

Após o fechamento técnico inicial, a suíte completa identificou testes legados que ainda esperavam
LONG / SHORT no payload canônico vindo do fallback RTD legado.

Decisão tomada:

- Manter o contrato canônico de estrutura em COMPRADO / VENDIDO.
- Preservar LONG / SHORT apenas nas bordas técnicas de pricing/cálculo.
- Atualizar somente as expectativas dos testes legados, sem alteração funcional em produção.

Arquivos de teste ajustados:

- ATT/tests/test_legacy_structure_legs_reader.py
- ATT/tests/test_rtd_legacy_canonical_pricing_input_guardrail.py

Testes executados:

    python -m pytest ATT/tests/test_legacy_structure_legs_reader.py ATT/tests/test_rtd_legacy_canonical_pricing_input_guardrail.py

Resultado:

    5 passed in 1.29s

Teste regressivo completo:

    python -m pytest

Resultado:

    610 passed, 10 skipped in 36.66s

Commit relacionado:

    4ed742e test: align legacy legs expectations with canonical position side

