===== DATA =====
2026-07-09 09:31:42

===== OBJETIVO =====
Fase 1F-D.20: migrar script validate_derived_db.py para validate_app_db.py e remover residuos operacionais DERIVED_DB_PATH/get_derived_connection.

===== DIFF SUMMARY =====

===== DIFF =====
diff --git a/scripts/validate_app_db.py b/scripts/validate_app_db.py
index 45545a9..8f6af5a 100644
--- a/scripts/validate_app_db.py
+++ b/scripts/validate_app_db.py
@@ -1,91 +1,63 @@
-#!/usr/bin/env python3
+#!/usr/bin/env python
 """
-Script para validar consistencia do banco app.db
-- Nao depende de get_derived_connection (que pode nao existir no seu layout)
-- Faz auto-discovery do caminho do app.db
+Valida o banco unico da aplicacao app.db.
+
+Regras:
+- usa APP_DB_PATH, se definido;
+- caso contrario, usa dados/app.db a partir da raiz do projeto;
+- nao depende de funcoes de conexao legadas.
 """
+
 from __future__ import annotations
 
 import os
-import sys
 import sqlite3
+import sys
 from pathlib import Path
 
-PROJECT_ROOT = Path(__file__).resolve().parents[1]
-if str(PROJECT_ROOT) not in sys.path:
-    sys.path.insert(0, str(PROJECT_ROOT))
 
-from db.derived_repo import validate_snapshot_consistency
+PROJECT_ROOT = Path(__file__).resolve().parent.parent
+DEFAULT_APP_DB_PATH = PROJECT_ROOT / "dados" / "app.db"
 
 
-def _find_app_db() -> Path | None:
-    # 1) env (prioridade)
-    env = os.environ.get("DERIVED_DB_PATH") or os.environ.get("DERIVED_DB") or os.environ.get("DERIVED_DB_FILE")
+def find_app_db_path() -> Path:
+    env = os.environ.get("APP_DB_PATH")
     if env:
-        p = Path(env)
-        if p.exists():
-            return p
-
-    # 2) caminhos padrao comuns no repo
-    candidates = [
-        PROJECT_ROOT / "dados" / "app.db",
-        PROJECT_ROOT / "data" / "app.db",
-        PROJECT_ROOT / "app.db",
-        PROJECT_ROOT / "db" / "app.db",
-    ]
-    for p in candidates:
-        if p.exists():
-            return p
-    return None
+        return Path(env).expanduser().resolve()
+    return DEFAULT_APP_DB_PATH.resolve()
 
 
-def main() -> int:
-    print("=== VALIDACAO DO BANCO APP.DB ===")
+def validate_app_db(path: Path) -> int:
+    print(f"[INFO] APP_DB_PATH: {path}")
 
-    db_path = _find_app_db()
-    if not db_path:
-        print("[ERROR] app.db nao encontrado.")
-        print("        Defina DERIVED_DB_PATH (ou DERIVED_DB) apontando para o arquivo.")
+    if not path.exists():
+        print("[ERRO] Banco app.db nao encontrado.")
+        print("       Defina APP_DB_PATH apontando para o arquivo correto.")
         return 2
 
-    print(f"[INFO] Usando app.db em: {db_path}")
-
-    conn = sqlite3.connect(str(db_path))
     try:
-        cur = conn.cursor()
-
-        # Estatisticas basicas (se tabelas existirem)
-        def safe_count(table: str) -> int | None:
-            try:
-                cur.execute(f"SELECT COUNT(*) FROM {table}")
-                return int(cur.fetchone()[0])
-            except Exception:
-                return None
-
-        points_count = safe_count("payoff_curve_points")
-        decisions_count = safe_count("structure_decisions")
-
-        if points_count is not None:
-            print(f"[INFO] Pontos: {points_count}")
-        else:
-            print("[WARN] Tabela payoff_curve_points nao acessivel (ou nao existe).")
-
-        if decisions_count is not None:
-            print(f"[INFO] Decisoes: {decisions_count}")
-        else:
-            print("[WARN] Tabela structure_decisions nao acessivel (ou nao existe).")
-
-        print("[INFO] Validando consistencia (pontos <-> decisoes)...")
-        ok = validate_snapshot_consistency(conn)
-
-        if ok:
-            print("[OK] BANCO ESTA CONSISTENTE")
-            return 0
-        else:
-            print("[ERROR] BANCO POSSUI INCONSISTENCIAS")
-            return 1
-    finally:
-        conn.close()
+        with sqlite3.connect(str(path)) as conn:
+            cur = conn.cursor()
+            cur.execute("SELECT name FROM sqlite_master WHERE type='table' ORDER BY name")
+            tables = [row[0] for row in cur.fetchall()]
+    except Exception as exc:
+        print(f"[ERRO] Falha ao abrir/consultar banco: {exc}")
+        return 3
+
+    print(f"[OK] Banco acessivel: {path}")
+    print(f"[INFO] Total de tabelas: {len(tables)}")
+
+    if tables:
+        print("[INFO] Tabelas:")
+        for name in tables:
+            print(f"  - {name}")
+
+    return 0
+
+
+def main() -> int:
+    path = find_app_db_path()
+    return validate_app_db(path)
 
 
 if __name__ == "__main__":

===== EXECUCAO SCRIPT NOVO =====
[INFO] APP_DB_PATH: C:\Users\eucal\projeto\dados\app.db
[OK] Banco acessivel: C:\Users\eucal\projeto\dados\app.db
[INFO] Total de tabelas: 13
[INFO] Tabelas:
  - payoff_curve_points
  - pricing_executions
  - rtd_option_quotes
  - rtd_option_quotes_backup_scope_20260626_184057
  - rtd_underlying_quotes
  - sqlite_sequence
  - structure_audit_log
  - structure_decisions
  - structure_events
  - structure_leg_snapshots
  - structure_legs
  - structure_snapshots
  - structures

===== GREP TOKENS LEGADOS EM CODIGO OPERACIONAL =====

===== GREP NOMES DE ARQUIVO LEGADOS =====
scripts/repair_derived_db_consistency.py
scripts/__pycache__/repair_derived_db_consistency.cpython-313.pyc
scripts/__pycache__/validate_derived_db.cpython-313.pyc

===== CONTEUDO scripts/validate_app_db.py =====
0001: #!/usr/bin/env python
0002: """
0003: Valida o banco unico da aplicacao app.db.
0004: 
0005: Regras:
0006: - usa APP_DB_PATH, se definido;
0007: - caso contrario, usa dados/app.db a partir da raiz do projeto;
0008: - nao depende de funcoes de conexao legadas.
0009: """
0010: 
0011: from __future__ import annotations
0012: 
0013: import os
0014: import sqlite3
0015: import sys
0016: from pathlib import Path
0017: 
0018: 
0019: PROJECT_ROOT = Path(__file__).resolve().parent.parent
0020: DEFAULT_APP_DB_PATH = PROJECT_ROOT / "dados" / "app.db"
0021: 
0022: 
0023: def find_app_db_path() -> Path:
0024:     env = os.environ.get("APP_DB_PATH")
0025:     if env:
0026:         return Path(env).expanduser().resolve()
0027:     return DEFAULT_APP_DB_PATH.resolve()
0028: 
0029: 
0030: def validate_app_db(path: Path) -> int:
0031:     print(f"[INFO] APP_DB_PATH: {path}")
0032: 
0033:     if not path.exists():
0034:         print("[ERRO] Banco app.db nao encontrado.")
0035:         print("       Defina APP_DB_PATH apontando para o arquivo correto.")
0036:         return 2
0037: 
0038:     try:
0039:         with sqlite3.connect(str(path)) as conn:
0040:             cur = conn.cursor()
0041:             cur.execute("SELECT name FROM sqlite_master WHERE type='table' ORDER BY name")
0042:             tables = [row[0] for row in cur.fetchall()]
0043:     except Exception as exc:
0044:         print(f"[ERRO] Falha ao abrir/consultar banco: {exc}")
0045:         return 3
0046: 
0047:     print(f"[OK] Banco acessivel: {path}")
0048:     print(f"[INFO] Total de tabelas: {len(tables)}")
0049: 
0050:     if tables:
0051:         print("[INFO] Tabelas:")
0052:         for name in tables:
0053:             print(f"  - {name}")
0054: 
0055:     return 0
0056: 
0057: 
0058: def main() -> int:
0059:     path = find_app_db_path()
0060:     return validate_app_db(path)
0061: 
0062: 
0063: if __name__ == "__main__":
0064:     raise SystemExit(main())

===== COMPILEALL =====
Listing 'db'...
Listing 'db\\migrations'...
Listing 'scripts'...
Listing 'services'...
Listing 'ui'...
Listing 'ui\\components'...
Listing 'ui\\models'...
Listing 'ui\\modern'...
Listing 'tests'...
Can't list 'tests'

===== PYTEST =====

[conftest] PROJECT_ROOT : C:\Users\eucal\projeto
[conftest] DB_PATH      : C:\Users\eucal\projeto\dados\app.db
[conftest] DB existe?   : True
........................................................................ [ 12%]
........................................................................ [ 24%]
........................................................................ [ 36%]
.................................................................. [ 47%]
........................................................................ [ 59%]
........................................................................ [ 72%]
.............ss......................................................... [ 84%]
........................................................................ [ 96%]
.....................                                                    [100%]
589 passed, 2 skipped, 6 subtests passed in 32.81s

===== GIT STATUS =====
RM scripts/validate_derived_db.py -> scripts/validate_app_db.py
?? FRENTE_BD_UNICO_APPDB/evidencias/132_phase1f_d20_migrate_validate_app_db_script.txt
